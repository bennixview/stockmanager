package com.trianel.masterdata.market;

import com.trianel.masterdata.market.dto.MarketDTO;
import com.trianel.masterdata.market.dto.MarketMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.event.EventListener;

/**
 * Just an Example
 */
public class MarketRabbitListener {

/*
    private final MarketService marketService;
    private final MarketMapper marketMapper;

    @Autowired
    public MarketRabbitListener(MarketService marketService, MarketMapper marketMapper) {
        this.marketService = marketService;
        this.marketMapper = marketMapper;
    }

    @EventListener
    public void listenToMessagingg(MarketDTO marketDTO) {
        marketService.createOrUpdateMarket(marketMapper.toDomain(marketDTO);
    }*/

}
