package com.bewi.stockmanager.marketdata;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Boot entry point for adapter tests.
 *
 * <p>The infrastructure module has no application of its own - the bootstrap module is the runnable
 * one - so slice tests need something to anchor component scanning to.
 */
@SpringBootApplication
class InfrastructureTestApplication {
}
